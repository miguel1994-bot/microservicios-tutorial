server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          deafult-label: config-server
          uri: https://github.com/miguel1994-bot/microservicios-tutorial
          search-paths: config-data 
  application:
    name: config-service  
